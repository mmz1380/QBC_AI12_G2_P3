# Python 3.11 (.venv). Install with:
#   ./.venv/Scripts/python -m pip install -r requirements.txt
#
# For the GPU embedding/LLM build, install torch FIRST from the CUDA index:
#   ./.venv/Scripts/python -m pip install torch --index-url https://download.pytorch.org/whl/cu121

# ---- core / Phase 1: data acquisition, cleaning, EDA ----
pandas==2.2.3
numpy>=1.26
pyarrow>=15               # streaming parquet IO + chunked CSV reading
huggingface-hub>=0.25     # download the pinned dataset revision
hazm==0.10.0              # Persian normalizer / tokenizer (optional; graceful fallback)
persiantools>=5.0         # Jalali(Persian) -> Gregorian date conversion
plotly>=7.0               # all plots (works inside Streamlit)
python-dotenv>=1.0        # loads .env (API keys) -- see .env.sample

# ---- Phase 2: retrieval / embeddings / LLM ----
torch>=2.4                # +cu121 build; compute backend for embeddings and local LLM
sentence-transformers>=3.0   # local multilingual embeddings (MiniLM)
transformers>=4.44        # local Qwen LLM (RUN_MODE=local)
accelerate>=0.30          # device_map for the local LLM
requests>=2.31            # hosted OpenAI-compatible APIs (Groq / OpenRouter / paid)

# ---- Phase 3: recommendation_status classifier ----
scikit-learn>=1.4         # TF-IDF + linear model + Macro-F1
joblib>=1.3               # model persistence
peft>=0.11                # LoRA fine-tuning (bonus: Phase 3 LoRA vs. TF-IDF baseline)

# ---- Phase 4 + dashboard ----
scipy>=1.11               # Spearman for judge/human agreement
streamlit>=1.36           # interactive dashboard
jupyter>=1.0              # notebooks
ipykernel>=6.29
nbconvert>=7.0            # execute the standalone notebook headless
pytest>=8.0              # tests/  (python run.py test)
